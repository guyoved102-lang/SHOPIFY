<style>
  /* ── SockAcademy Logo — Editorial Masthead ── */
  .sa-logo {
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
    line-height: 1;
    flex-shrink: 0;
    gap: 0;
    user-select: none;
  }

  .sa-logo__top {
    display: flex;
    align-items: baseline;
    gap: 0;
    line-height: 1;
  }

  .sa-logo__sock {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: 2rem;
    font-weight: 700;
    font-style: italic;
    color: var(--cream);
    letter-spacing: 0.04em;
    line-height: 1;
  }

  .sa-logo__dot {
    width: 5px;
    height: 5px;
    background: var(--gold);
    border-radius: 50%;
    margin: 0 0.3rem 0.25rem;
    flex-shrink: 0;
    align-self: flex-end;
  }

  .sa-logo__rule {
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--muted) 20%, var(--muted) 80%, transparent);
    margin: 0.18rem 0;
    display: block;
  }

  .sa-logo__academy {
    font-family: 'DM Sans', 'Helvetica Neue', Arial, sans-serif;
    font-size: 0.6rem;
    font-weight: 700;
    color: var(--muted);
    letter-spacing: 0.45em;
    text-transform: uppercase;
    line-height: 1;
    padding-right: 0.45em; /* optical compensation for letter-spacing */
  }

  /* ── Scroll shrink ── */
  .scrolled-past-header .sa-logo__sock {
    font-size: 1.55rem;
    transition: font-size 0.2s ease;
  }

  .scrolled-past-header .sa-logo__academy {
    font-size: 0.5rem;
    transition: font-size 0.2s ease;
  }

  /* ── Mobile ── */
  @media screen and (max-width: 749px) {
    .sa-logo__sock {
      font-size: 1.6rem;
    }

    .sa-logo__academy {
      font-size: 0.5rem;
      letter-spacing: 0.35em;
    }
  }
</style>

<a href="{{ routes.root_url }}" class="sa-logo" aria-label="{{ shop.name }}">
  <div class="sa-logo__top">
    <span class="sa-logo__sock">SockAcademy</span>
    <span class="sa-logo__dot" aria-hidden="true"></span>
  </div>
  <span class="sa-logo__rule" aria-hidden="true"></span>
  <span class="sa-logo__academy">The Sock Authority</span>
</a>
